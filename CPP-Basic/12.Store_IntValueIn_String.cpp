#include<bits/stdc++.h>
using namespace std;

int main()
{
    //int element
    int a = 12345;
    cout<<"Value in a: "<<a<<endl;
    //store a's value in a string
    string str = to_string(a); // "to_string(value)" is a string function to store a non character value in string;
    cout<<"After storing a's value in str: "<<str<<endl;
}

